/** Màu trang trí biểu đồ — không phải màu semantic của metadata (`class_learning_type`). */
export const LEARNING_TYPE_COLORS: Record<string, string> = {
  scheduled: "#0ea5e9",
  self_learning: "#8b5cf6",
  flexible: "#f59e0b",
};
