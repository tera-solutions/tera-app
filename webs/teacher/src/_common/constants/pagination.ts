/** Shared page-size choices for every paginated list across the app. */
export const PAGE_SIZE_OPTIONS = [10, 20, 50, 100];

/** Shared default page size for every paginated list across the app. */
export const DEFAULT_PAGE_SIZE = 20;
