// Moved to `@tera/components/dof/StatusBadge` so every portal (admin/teacher/
// parent/student) reads the same backend-driven status metadata. Re-exported
// here so existing imports in this app don't need to change.
export { default } from "@tera/components/dof/StatusBadge";
