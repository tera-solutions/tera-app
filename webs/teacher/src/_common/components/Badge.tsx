// Moved to `@tera/components/dof/Badge` so every portal (admin/teacher/parent/
// student) shares one implementation. Re-exported here so existing imports
// in this app don't need to change.
export { default } from "@tera/components/dof/Badge";
