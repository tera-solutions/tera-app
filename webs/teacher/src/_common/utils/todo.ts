import { notification } from "tera-dls";

/** Placeholder handler for actions that aren't implemented yet. */
export const todo = () =>
  notification.open({ message: "Tính năng đang được phát triển" });
